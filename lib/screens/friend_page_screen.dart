import 'package:flutter/material.dart';

class FriendPageScreen extends StatelessWidget {
  const FriendPageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('친구 페이지 내용'),
    );
  }
}
